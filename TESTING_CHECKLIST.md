# แผ่นสำรวจ/เช็คลิสต์ทดสอบระบบไอเทม (หลังปรับคูลดาวน์ + พฤติกรรมไอเทม)

ใช้เช็คก่อนนำไปเล่นจริงในห้องเรียน ทำเครื่องหมาย ✅ เมื่อลองแล้วได้ผลตามที่คาด
ถ้าอันไหนไม่ตรง ให้จดพฤติกรรมจริงที่เห็นไว้ด้วย จะได้แก้ตรงจุด

## ค่าที่ปรับใหม่ (server.js → ITEM_COOLDOWNS)
| ไอเทม | คูลดาวน์ (หลังใช้ 1 ครั้ง) | เอฟเฟกต์กินเวลา |
|---|---|---|
| สลับไพ่ (swap) | 10 วิ | ทันที (ไม่มีระยะเอฟเฟกต์) |
| แช่แข็ง (freeze) | 10 วิ | 5 วิ (บล็อกฝั่งตรงข้าม) |
| ส่องไพ่ (peek) | 30 วิ | 3 วิ (เปิดไพ่คว่ำทั้งหมดของทีมตัวเอง) |

คูลดาวน์เป็น **แบบต่อไอเทม** (แยกอิสระจากกัน) — ใช้ "ส่องไพ่" ไปแล้วรอ 30 วิ
ไม่ได้แปลว่า "สลับ" หรือ "แช่แข็ง" จะโดนล็อกไปด้วย ปุ่มไหนใช้ไปก็คูลดาวน์เฉพาะปุ่มนั้น
ปุ่มอื่นยังกดได้ตามปกติ (ถ้ามีโทเค็นพอ)

## 1) ไอเทมสลับไพ่ (swap)
- [ ] ทีม A จับคู่ไพ่ถูกไปแล้ว 1 คู่ → ทีม B ใช้สลับใส่ทีม A
      → คาดว่า: ไพ่คู่ที่จับถูกของทีม A **ปิดกลับเป็นคว่ำ** และไพ่คว่ำ**ทั้งหมด**บนกระดานของทีม A ถูกสลับตำแหน่งใหม่
- [ ] ทีม A มีไพ่ที่จับถูกหลายคู่ → ใช้สลับซ้ำ → คาดว่า: ปิดกลับทีละ 1 คู่ต่อการใช้ 1 ครั้ง (ไม่ปิดรวดเดียวหมด)
- [ ] ทีม A ยังไม่จับคู่ถูกเลยสักคู่ แต่กำลังเปิดไพ่ 2 ใบรอโหวตอยู่ (ค้างหน้าโหวต) → ทีม B ใช้สลับ
      → คาดว่า: ไพ่ 2 ใบที่ค้างเปิดอยู่ปิดกลับเป็นคว่ำ, popup โหวตของทีม A ปิดตัวเองอัตโนมัติ, ไพ่คว่ำทั้งหมดถูกสลับ
- [ ] ทีม A ยังไม่จับคู่ถูกเลย และไม่มีไพ่ค้างเปิดด้วย → ใช้สลับ
      → คาดว่า: ไพ่คว่ำทั้งหมดถูกสลับตำแหน่งเฉยๆ (ไม่มีอะไรถูก "ปิด" เพราะไม่มีอะไรเปิดอยู่)
- [ ] ทีม A จับคู่ครบทุกคู่แล้ว (ขึ้น "จับคู่ครบแล้ว!") → ทีม B ใช้สลับใส่
      → คาดว่า: ปิดกลับ 1 คู่ → ทีม A **หลุดจากสถานะจบเกม** กลับมาเล่นต่อได้ (matchedPairs ลดลงต่ำกว่าเป้าหมาย)
- [ ] ลองเลือกทีมเป้าหมายเป็นทีมตัวเอง หรือทีมที่ไม่มีอยู่จริง → คาดว่า: ระบบไม่ให้ใช้ (แจ้ง "เลือกทีมเป้าหมายไม่ถูกต้อง")
- [ ] ไพ่บนกระดานไม่ควรมีชื่อซ้ำ/หมวดหมู่ซ้ำแบบผิดปกติหลังสลับหลายรอบ (สุ่มสลับเฉพาะภายในชนิดเดียวกัน ชื่อ↔ชื่อ, หมวดหมู่↔หมวดหมู่ เท่านั้น)

## 2) ไอเทมแช่แข็ง (freeze)
- [ ] ใช้แช่แข็งใส่ทีมอื่น → ฝั่งนั้นเปิดไพ่ไม่ได้ 5 วิ (คลิกไม่ติด ทั้งฝั่งเรากดเร็วและฝั่งเซิร์ฟเวอร์ก็ปฏิเสธเหมือนกัน)
- [ ] รีเฟรชหน้าเบราว์เซอร์ระหว่างโดนแช่แข็ง → กลับมาแล้วยังต้องล็อกจนครบเวลาจริง (ไม่ใช่หลุดล็อกทันทีเพราะรีเฟรช)
- [ ] คูลดาวน์ 10 วิ หลังใช้แช่แข็ง — เฉพาะปุ่มแช่แข็งเท่านั้นที่ล็อก ปุ่มสลับ/ส่องไพ่ยังกดได้ถ้ามีโทเค็นพอ

## 3) ไอเทมส่องไพ่ (peek)
- [ ] กดส่องไพ่ → ไพ่คว่ำ**ทุกใบ**บนกระดานทีมตัวเองเปิดหน้าให้เห็น 3 วิ แล้วปิดกลับเอง
- [ ] ไพ่ที่ "จับคู่ถูกแล้ว" (เปิดค้างถาวร) หรือกำลังค้างเปิดรอโหวตอยู่ ไม่ควรถูกกระทบ/เปลี่ยนสถานะจากไอเทมนี้
- [ ] กดส่องไพ่ตอนกระดานเหลือไพ่คว่ำใบเดียว/ไม่เหลือเลย → ไม่ควร error หรือค้าง
- [ ] คูลดาวน์ 30 วิ หลังใช้ส่องไพ่ — เฉพาะปุ่มส่องไพ่เท่านั้นที่ล็อก ปุ่มสลับ/แช่แข็งยังกดได้ถ้ามีโทเค็นพอ
- [ ] คนอื่นในห้อง (ทีมอื่น) ไม่ควรเห็นไพ่ของทีมที่ส่องถูกเปิดค้าง (peek เป็นของทีมตัวเองเท่านั้น)

## 4) คูลดาวน์ + การเชื่อมต่อใหม่ (reconnect)
- [ ] ใช้ไอเทมแล้วรีเฟรชหน้าเบราว์เซอร์ทันที (ระหว่างยังคูลดาวน์ค้างอยู่)
      → คาดว่า: กลับเข้ามาแล้วปุ่มไอเทมยังโชว์แถบคูลดาวน์ค้างต่อ ไม่ใช่โชว์ว่า "พร้อมใช้งาน" ทั้งที่จริงยังใช้ไม่ได้
- [ ] โทเค็นไม่พอ → กดไอเทมไม่ได้ พร้อมข้อความแจ้งจำนวนโทเค็นที่ต้องการ
- [ ] กดรัวๆ ระหว่างคูลดาวน์ → ฝั่งเซิร์ฟเวอร์ปฏิเสธเสมอ (ปุ่มฝั่งไคลเอนต์ก็ล็อกไว้แล้วเป็นด่านแรก)

## 5) พื้นหลังรูปวิ่ง (dvd bounce)
- [ ] เปิดหน้าเว็บทิ้งไว้สัก 1–2 นาที → รูปทั้ง 11 ใบควรกระจายทั่วจอ ไม่จับกลุ่มติดกันเป็นพวง
- [ ] ลองเปิดหลายแท็บ/รีเฟรชหลายรอบ → ตำแหน่งเริ่มต้นควรกระจาย ไม่กระจุกมุมเดียวกันซ้ำๆ
- [ ] ความเร็วโดยรวมยังดูช้า/นุ่มนวลเหมือนเดิม ไม่มีรูปไหนพุ่งเร็วผิดปกติ (มีการจำกัดความเร็วสูงสุดไว้แล้ว)

## หมายเหตุ
- ปรับตัวเลขคูลดาวน์/ระยะเอฟเฟกต์ได้เองที่ต้นไฟล์ `server.js`:
  `ITEM_COOLDOWNS`, `FREEZE_DURATION_MS`, `PEEK_DURATION_MS`
- ทุกกฎ (โทเค็น, คูลดาวน์, เป้าหมายถูกต้องไหม) ถูกตรวจสอบที่ฝั่งเซิร์ฟเวอร์เป็นหลักเสมอ
  ฝั่งไคลเอนต์แค่ล็อกปุ่มไว้ล่วงหน้าเพื่อ UX ที่ลื่นขึ้นเท่านั้น
